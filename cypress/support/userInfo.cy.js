function UserInfo() {
  const userName = `alex100`
  const userEmail = `qaalex100@gmail.com`;
  const userPassword = `!@#Qtv`;
  const articleBody = `a`;
  const articleDescription = `a`;
  const articleTitle = `a`;
  const articleTags = `a`;
  const newArticleTitle = 'b';
  const userEmail2 = `12.12.123@inbox.ru`
  const userPassword2 = `!@#Qtv`
  
    return {userEmail2, userPassword2, userName, userEmail, userPassword, articleBody, articleDescription, articleTitle, articleTags, newArticleTitle};
  }
  
  module.exports = { UserInfo };